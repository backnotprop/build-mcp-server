interface:
  display_name: "Build MCP Server"
  short_description: "Design and scaffold MCP servers"
  default_prompt: "Use $build-mcp-server to design and build an MCP server."

policy:
  allow_implicit_invocation: true
